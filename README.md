# Simple Django Vue CRUD app

Create Project

```
docker-compose up

docker-compose run web migrate
```

Add to .vscode/settings.json

```
  "files.associations": {
    "*.html": "django-html",
    "**/requirements{/**,*}.{txt,in}": "pip-requirements",
  },
```
